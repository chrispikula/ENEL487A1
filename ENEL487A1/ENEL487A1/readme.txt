To compile this assignment, use the command
'make'
It currently compiles using -Wall
----------------------------------------------
This application can run in either batch mode,
or in regular mode.
To run in batch mode, run the command
./complex inputfile outputfile
To run in regular mode, run the command
./complex
